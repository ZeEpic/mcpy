def pythagorean_theorem(a: num, b: num, c: num): bool {
    return sqrt(a**2 + b**2) == c**2
}

# If you aren't done with a function and it returns something, write 'pass' or '...'
def i_am_not_done(): str {
    pass
}
