package com.rimlang.rim.translation.context

import com.rimlang.rim.translation.identifier.Identifier

class GlobalContext(identifiers: MutableList<Identifier> = mutableListOf()) : Context(identifiers)
